# IsolationForest-Replication
IsolationForest-Replication is a minimal implementation of the Isolation Forest algorithm based directly on the original paper. It focuses on reproducing the core idea of anomaly detection through random partitioning and path length–based scoring without relying on external libraries.
